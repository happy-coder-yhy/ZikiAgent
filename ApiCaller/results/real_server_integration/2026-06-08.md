# ZataAPICaller Real Server Integration - 2026-06-08

This is a one-off validation record for the real test platform. Stable testing rules live in `docs/development/testing.md`.

## Environment

- Base URL: `http://pre.zikirobo.com:30080/`
- Organization: `agent`
- User: `admin`
- Request mode: serial only, with visible `2.1` second delays between API requests.
- Test resource prefix: `agent_test_` for manual validation resources only. This prefix is not enforced by `ZataAPICaller`.

## Smoke Read Validation

The read-only smoke test completed successfully:

1. `login`: HTTP 200, `code=0`, access token returned.
2. `userinfo`: HTTP 200, `code=0`.
3. `list_projects`: HTTP 200, `code=0`.
4. `list_label_categories`: HTTP 200, `code=0`, returned `scene`, `space`, `task`, and `device`.
5. `get_label_tree("scene")`: HTTP 200, `code=0`, returned scene labels including `id=300` (`居家`).

## Platform Configuration Snapshot Validation

The read-only Platform Configuration Snapshot was validated against the current initialization files:

- `configs/zata_init/场景标签.xlsx`
- `configs/zata_init/空间标签.xlsx`
- `configs/zata_init/任务标签.xlsx`
- `configs/zata_init/设备标签.xlsx`

Request mode remained serial, with visible `2.1` second waits between requests. The validation request sequence was:

1. `POST /api/zata-rbac/login`
2. `GET /api/zata-manager/projects?pageNum=1&pageSize=200`
3. `GET /api/zata-manager/tasks?pageNum=1&pageSize=200`
4. `GET /api/zata-manager/label-categories`
5. `GET /api/zata-manager/labels/tree?categoryCode=scene`
6. `GET /api/zata-manager/labels/tree?categoryCode=space`
7. `GET /api/zata-manager/labels/tree?categoryCode=task`
8. `GET /api/zata-manager/labels/tree?categoryCode=device`
9. `GET /api/zata-manager/object-categories`
10. `GET /api/zata-manager/object-items?categoryId=<each flattened object category id>&pageNum=1&pageSize=200`

Validation result:

| Category | Expected | Actual | Missing | Extra |
| --- | ---: | ---: | ---: | ---: |
| `scene` | 13 | 13 | 0 | 0 |
| `space` | 13 | 13 | 0 | 0 |
| `task` | 108 | 108 | 0 | 0 |
| `device` | 21 | 21 | 0 | 0 |

Object library validation:

| Expected objects | Actual objects | Missing | Extra |
| --- | --- | --- | --- |
| `香蕉`, `苹果`, `扫码枪`, `扫码台` | `香蕉`, `苹果`, `扫码枪`, `扫码台` | none | none |

Snapshot counts observed:

- Projects: 1
- Tasks: 0
- Label categories: 4
- Flattened labels: 155
- Flattened object categories: 4
- Object items: 4

Important implementation observation: a root-level `GET /object-items` query can return no objects. Object items must be fetched by first reading the object category tree, flattening root and child category IDs, then querying `/object-items` per `categoryId`.

## Full API Caller Workflow Validation

The full write workflow was validated with test-prefixed resources:

1. `create_project`: success, created `projectId=2`.
2. `create_task`: success, created `taskId=4` under the project.
3. `update_task_keep_jobs`: success.
4. `create_jobs`: success, created `jobId=10`.
5. `get_job`: success.
6. `update_job`: success.
7. `publish_task`: success.
8. `unpublish_task`: success.
9. `archive_task`: success.
10. `unarchive_task`: success.
11. `delete_jobs`: success, deleted `jobId=10`.
12. `delete_task`: initially failed with `code=17105025`, `reason=TASK_PUBLISHED_CANNOT_DELETE`.
13. `get_task`: showed `status=2`, so the platform still considered the task published.
14. `unpublish_task`: success, returned task detail with `status=1`.
15. `get_task`: confirmed `status=1`.
16. `update_task`: success with explicit `status=1`.
17. `delete_task`: success, deleted `taskId=4`.
18. `delete_project`: success, deleted `projectId=2`.

## Observed Platform Rule

After release-state operations, task deletion should be guarded by a status check. If task detail reports `status=2`, call `unpublish_task`, then query task detail again and only delete after `status=1`.

