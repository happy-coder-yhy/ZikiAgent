# Documentation-Backed Code Development

New runtime capabilities should be backed by a corresponding standard document before or alongside implementation. This keeps code changes tied to an explicit API, verifier, workflow, or domain contract instead of allowing ad hoc modules and behavior to appear without a documented purpose.
