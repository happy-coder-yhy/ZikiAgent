# Split API Documentation By Role And Capability

API documentation should live under `docs/api/` with `index.md` as the navigation entry and separate pages for platform configuration, operator, collector, and audit/archive operations. The primary retrieval structure should be role and capability, not HTTP endpoint order, because users look for what they need to do before they look for the platform path that implements it.
